# لعبة جدول الضرب (واجهة عربية)

تطبيق ويب بسيط يعمل محليًا في المتصفح لاختبار طلاب الصف ذوي اللغة العربية في جداول الضرب من **٢ إلى ٩**.

## المزايا
- شاشة رئيسية بعنوان **"لعبة جدول الضرب"** وشبكة من **٤٠ مربعًا** تمثل الطلاب.
- كل مربع يبدأ برمز **🔒** ويصبح **✅** بعد إكمال الاختبار.
- عند النقر على مربع طالب، تُفتح نافذة مع **٥ أسئلة عشوائية** (بدون تكرار) من جدول ٢ إلى ٩.
- الأسئلة تُعرض **واحدًا تلو الآخر** بأرقام عربية (مثال: `٥ × ٨`).
- لكل سؤال **أربع خيارات**، أحدها صحيح وثلاثة مشتتات منطقية.
- عدّاد درجة مرئي: **`الدرجة: ٠/٥`** يتحدّث بعد كل إجابة صحيحة.
- بعد إتمام الأسئلة الخمسة، تُعرض النتيجة النهائية (مثل: **`ممتاز! نتيجتك هي ٥/٥`**) ثم تُغلق النافذة تلقائيًا.
- يتم حفظ حالة إكمال الطالب في **localStorage** لتظهر في الشبكة حتى بعد إعادة التحميل.
- مؤثرات صوتية: شغّل **`win.mp3`** للإجابة الصحيحة و **`lose.mp3`** للإجابة الخاطئة.

## المتطلبات
- ضع ملفي الصوت **`win.mp3`** و **`lose.mp3`** في نفس مجلد الملفات بجوار `index.html`.
- لا توجد مكتبات خارجية، فقط **HTML/CSS/JS**.

## طريقة التشغيل
1. افتح الملف `index.html` في أي متصفح حديث (Chrome, Edge, Firefox …).
2. انقر على أي مربع طالب لبدء الاختبار.

## ملاحظات تصميمية
- الألوان زاهية وودية للأطفال، مع زوايا مستديرة وظلال لطيفة.
- كل النصوص والأرقام المعروضة للمستخدم بالعربية.
- تم استخدام ترميز **RTL** واتجاه الصفحة **`dir="rtl"`**.

## رفع إلى GitHub
1. أنشئ مستودعًا جديدًا على GitHub.
2. من الطرفية داخل المجلد:
   ```bash
   git init
   git add .
   git commit -m "إضافة لعبة جدول الضرب العربية"
   git branch -M main
   git remote add origin https://github.com/USERNAME/REPO.git
   git push -u origin main
   ```

> ملاحظة: يمكنك تعديل عدد الطلاب، الرسائل، والألوان بسهولة عبر الملف `script.js` و`style.css`.
